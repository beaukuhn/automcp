project:
  name: "test"
  version: "1.0.0"
  defaultLanguage: "typescript"
  defaultTransport: "stdio"

source: "service/**/*.openmpc.yaml"

generates:
  "generated/":
    plugins:
      - "server"
      - "client"
      - "openapi"
